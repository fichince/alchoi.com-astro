---
import { Picture } from '@astrojs/image/components';

interface Props {
  src: any,
  alt: string,
}

const { src, alt } = Astro.props;
---
<Picture src={src}
  alt={alt}
  widths={[350, 500]}
  sizes="(min-width: 480px) 500px, 350px"
/>
